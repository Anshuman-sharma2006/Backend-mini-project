//   npx nodemon Script.js <- this is after changing in code automatically reload otherwise (node script.js)
const app=require('./src/App')


 app.listen(3000,()=>{console.log("Here we Start the connection");
 })// conncet the host port that we are going to perform task  